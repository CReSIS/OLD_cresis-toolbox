
% Create trajectory and attitude (gps file, records file)
% start point
% sim.traj_linear.m linear path (length) or sim.traj_circ.m circular path (radius)
%   linear: heading and length, start and stop elevation, RMS variation, correlation length, sample rate
  
  
% 

% Create targets
%   Line at nadir: start/stop elevation, RMS variation, correlation length, sample rate
%   Import DEM: filtering?
%   Surface generator (point target, factet)
%   Dielectric layer generator... thickness but also constrain variation so
%     random walk does not get out of control

% Create simulated data (specify each waveform)
%   Loop over each phase center
%   ADC: bits, full scale, quantization power
%   Rx gain (time delay, phase shift, amplitude)
%   Sampling frequency, PRF
%   Echo/direct receive or deramp on receive
%   transmit array: transmit power, lever arms
%   Waveform (linear FM: start, stop, pulse duration, fast time window)
%   





